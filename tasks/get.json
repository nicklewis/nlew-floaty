{
  "description": "Get a set of VMs from the vmpooler",
  "input_method": "stdin",
  "parameters": {
    "platform": {
      "description": "The kind of VM to get",
      "type": "String[1]"
    },
    "count": {
      "description": "The number of VMs to get (default: 1)",
      "type": "Integer[1]"
    },
    "plugin": {
      "description": "Whether to output bare string list, or list of inventory v2 compatible hashes",
      "type": "Optional[Boolean]"
    }
  }
}
